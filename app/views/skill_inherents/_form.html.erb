<%= form_for(@skill_inherent) do |f| %>
  <% if @skill_inherent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill_inherent.errors.count, "error") %> prohibited this skill_inherent from being saved:</h2>

      <ul>
      <% @skill_inherent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
