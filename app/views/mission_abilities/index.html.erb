<h1>Listing mission_abilities</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mission_abilities.each do |mission_ability| %>
  <tr>
    <td><%= link_to 'Show', mission_ability %></td>
    <td><%= link_to 'Edit', edit_mission_ability_path(mission_ability) %></td>
    <td><%= link_to 'Destroy', mission_ability, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mission ability', new_mission_ability_path %>
