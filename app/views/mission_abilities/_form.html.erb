<%= form_for(@mission_ability) do |f| %>
  <% if @mission_ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission_ability.errors.count, "error") %> prohibited this mission_ability from being saved:</h2>

      <ul>
      <% @mission_ability.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
