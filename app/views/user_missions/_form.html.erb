<%= form_for(@user_mission) do |f| %>
  <% if @user_mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_mission.errors.count, "error") %> prohibited this user_mission from being saved:</h2>

      <ul>
      <% @user_mission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
