<h1>Listing tasks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_missions.each do |user_mission| %>
  <tr>
    <td><%= link_to 'Show', user_mission %></td>
    <td><%= link_to 'Edit', edit_user_mission_path(user_mission) %></td>
    <td><%= link_to 'Destroy', user_mission, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User Mission', new_user_mission_path %>
