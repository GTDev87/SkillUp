<fieldset id="<%= user_mission.id %>">
  <% present user_mission do |user_mission_presenter|%>
    <%= user_mission_presenter.mission %>
  <% end %>
  
  <%= link_to "remove", user_user_mission_path(@user, user_mission), method: :delete, remote: true %>
</fieldset>
