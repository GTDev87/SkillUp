<%= form_for(@mission_embedding) do |f| %>
  <% if @mission_embedding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission_embedding.errors.count, "error") %> prohibited this mission_embedding from being saved:</h2>

      <ul>
      <% @mission_embedding.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
