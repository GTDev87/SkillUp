<h1>Listing mission_embeddings</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mission_embeddings.each do |mission_embedding| %>
  <tr>
    <td><%= link_to 'Show', mission_embedding %></td>
    <td><%= link_to 'Edit', edit_mission_embedding_path(mission_embedding) %></td>
    <td><%= link_to 'Destroy', mission_embedding, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mission embedding', new_mission_embedding_path %>
