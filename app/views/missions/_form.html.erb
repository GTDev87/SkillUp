<%= form_for(@mission) do |f| %>
  <% if @mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
      <% @mission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <h2>Mission Abilities</h2>
  <%= f.fields_for :mission_abilities do |mission_ability_builder| %>
      <%= render 'mission_ability_fields', f: mission_ability_builder %>
  <% end %>
  <%= link_to_add_fields "Add Ability", f, :mission_abilities %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
